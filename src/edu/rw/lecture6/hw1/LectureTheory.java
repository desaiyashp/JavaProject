package edu.rw.lecture6.hw1;
public class LectureTheory {
//    Types of Modifiers ---- 1. Access Modifier, 2. Non-access Modifier
/*           Access Modifier --- public (within Project, accessible by all classes),
                                 private (within declared class),
                                 protected (within package and subclasses),
                                 default (within package)

             Non-Access Modifier -- static
                                    final (value is fixed, not changing afterwards)
                                    abstract
                                    synchronized / volatile
*/

/*    Method = "A block of code grouped to perform certain task."
       Syntex =
        <AccessName> <ReturnType> <MethodName> <(ParameterList)> {
                   ....method body....
        }
      eg. public int calculate (int a, int b) {
          c = a+b;
          System.out.println(c);
          return c;
          }
*/

/*    String
        String is a collection of characters
        String class can be used using different methods;
           eg. String fullName = fstName.concat(lstName);
               int fullNameLength = fullName.length();


*/


}
